# 🚀 Multiverse Workbench 启动指南 (Startup Guide)

本项目包含 **Python 后端** (FastAPI) 和 **Vue 3 前端** (Vite)。
请打开两个终端窗口，分别启动它们。

---

## 🟢 1. 启动后端 (Backend)

后端负责大模型交互 (DeepSeek)、故事逻辑计算和数据存储。

### 步骤
1.  **进入目录**:
    ```powershell
    cd media/code/media-backend
    ```
2.  **激活环境** (如果未激活):
    ```powershell
    # 假设虚拟环境在 lab 根目录
    ..\..\..\.venv\Scripts\Activate.ps1
    ```
3.  **启动服务**:
    ```powershell
    # 推荐指定 host 和 port
    uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
    ```

### 成功标志
看到类似以下的输出：
```text
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

---

## 🔵 2. 启动前端 (Frontend)

⚠️ **注意**: 我们现在通过 `media-frontend2` (Vite 版本) 进行开发，不要去旧的 `media-frontend`。

### 步骤
1.  **进入目录**:
    ```powershell
    cd media/code/media-frontend2
    ```
2.  **启动开发服务器**:
    ```powershell
    npm run dev
    ```

### 成功标志
看到类似以下的输出：
```text
  VITE v5.x.x  ready in 300 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```
*(注意：如果 5173 被占用，Vite 会自动递增端口，如 5174, 5176 等，请留意终端输出)*

---

## 🌐 3. 访问项目

打开浏览器访问前端地址 (请根据实际终端输出的端口号，通常是 5173 或 5176)。

*   **🐢 海龟汤游戏 (Phase 5)**: `http://localhost:5176/game` (或 `/game`)
*   **🛠️ 多维工作台 (Workbench)**: `http://localhost:5176/workbench`
*   **API 文档**: `http://localhost:8000/docs`

---

## ⚠️ 常见问题

1.  **侦探罢工了 (404/Network Error)**:
    *   **原因**: 后端没启动，或者端口不是 8000。
    *   **解决**: 确保后端终端正在运行且没有报错。如果有“端口被占用”错误，请杀死旧的 `python` 进程。

2.  **页面白屏或报错**:
    *   **原因**: 依赖缺失。
    *   **解决**: 在 `media-frontend2` 下运行 `npm install`。

3.  **LLM 不回复**:
    *   **原因**: 缺少 API Key。
    *   **解决**: 检查 `.env` 文件中是否配置了 `DEEPSEEK_API_KEY`。如果没有，系统会使用 Mock 数据。
